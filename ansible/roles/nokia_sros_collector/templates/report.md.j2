# BÁO CÁO ĐO KIỂM ĐƯỜNG TRUYỀN - VATM ATCC LONG THÀNH
**Ngày:** {{ report_date }}  |  **Giờ thu thập:** {{ report_time }}  |  **Thực hiện bởi:** Ansible Automation

---

## TỔNG QUAN

| Chỉ số | Giá trị |
|--------|---------|
| Tổng số thiết bị | {{ ansible_play_hosts | length }} |
| Thu thập thành công | {{ collected_ok | default(0) }} |
| Thu thập lỗi/timeout | {{ collected_fail | default(0) }} |
| Số site có port DOWN | {{ sites_with_down_ports | default(0) }} |

---

## CHI TIẾT TỪNG THIẾT BỊ

{% for host in ansible_play_hosts %}
{% set m = hostvars[host].device_metrics | default({}) %}

### {{ loop.index }}. {{ m.hostname | default(host) }} — {{ m.site | default('N/A') }}

| Thông số | Giá trị |
|----------|---------|
| Địa chỉ IP quản trị | `{{ m.host_ip | default('N/A') }}` |
| Mô hình | {{ m.model | default('N/A') }} |
| Vai trò | {{ m.device_role | default('N/A') }} |
| Thời gian thu thập | {{ m.collected_at | default('N/A') }} |
| Trạng thái thu thập | {{ '✅ THÀNH CÔNG' if m.collection_status == 'SUCCESS' else '⚠️ KHÔNG ĐẦY ĐỦ' }} |

#### Port Status
```
{{ m.port_summary | default('Không lấy được dữ liệu') | truncate(800) }}
```

#### OSPF Neighbors
```
{{ m.ospf_neighbors | default('Không lấy được dữ liệu') | truncate(600) }}
```

#### SAA Results (Latency / Jitter)
```
{{ m.saa_results | default('Không có SAA test nào được cấu hình') | truncate(600) }}
```

{% if m.e1_ber is defined and 'N/A' not in m.e1_ber %}
#### E1 BER Statistics
```
{{ m.e1_ber | truncate(400) }}
```
{% endif %}

#### MPLS LSP Status
```
{{ m.mpls_lsp | default('N/A') | truncate(400) }}
```

---
{% endfor %}

## ĐÁNH GIÁ VÀ KHUYẾN NGHỊ

> *Báo cáo này được tạo tự động bởi hệ thống Ansible Monitoring.*
> *Kỹ thuật viên cần xem xét và đánh giá các cảnh báo bên dưới.*

### Các cảnh báo cần chú ý

{% for host in ansible_play_hosts %}
{% set ports_down = hostvars[host].ports_down | default([]) %}
{% if ports_down | length > 0 %}
- 🔴 **{{ host }}** ({{ hostvars[host].site | default('N/A') }}): Cổng DOWN — {{ ports_down | join(', ') }}
{% endif %}
{% endfor %}

---
*Tài liệu tham chiếu: HLD Hệ Thống Đường Truyền VATM LTIA — Hợp đồng 8225/HĐ-QLB*
